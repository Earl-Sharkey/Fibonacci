# Fibonacci
AppleScript – generates an n-length Fibonacci sequence or identifies the nth term

## ⚠️ Limitations

This script relies on AppleScript’s native numeric handling (floating-point arithmetic).

Because Fibonacci numbers grow exponentially, the script is limited to approximately **1476 terms**. Inputs beyond this limit are restricted to prevent overflow and invalid results.


Happy Fibing 🙂
